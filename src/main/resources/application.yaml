server:
  port: 8080


spring:
    datasource:
        password: ${RDBMS_PASSWORD}
        driver-class-name: org.postgresql.Driver
        url: jdbc:postgresql://${DATABASE_HOST:ec2-54-217-213-79.eu-west-1.compute.amazonaws.com}:${DATABASE_PORT:5432}/${DATABASE_NAME:d7gr8u6mejppks}?sslmode=require
        username: ${DATABASE_USER:myexdxofydyfwr}
        initialization-mode: always
        initialize: true
        schema: classpath:/schema.sql
        data: classpath:/data.sql
    jpa:
        hibernate:
            ddl-auto: none
        show-sql: true
        generate-ddl: false
        properties:
            hibernate:
                temp:
                    use_jdbc_metadata_defaults: false
                jdbc:
                    lob:
                        non_contextual_creation: true
                dialect: org.hibernate.dialect.PostgreSQL9Dialect